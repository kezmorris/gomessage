# Default values for gomessage-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image: morriski/gomessage:latest-master
pullPolicy: Always

service:
  type: LoadBalancer
  port: 8001

